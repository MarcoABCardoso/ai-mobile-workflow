using './main.bicep'

param projectName           = 'family-safe'
param location              = 'eastus'
param githubActionsClientId = '' // Fill in after creating the GitHub Actions service principal
