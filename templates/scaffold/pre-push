#!/usr/bin/env sh
# Pre-push hook: run build + tests before pushing to remote.
# Installed automatically via `npm run prepare` (postinstall).
# Skip with: git push --no-verify

set -e

echo "▶ pre-push: build + test"

npx turbo run build lint test --concurrency=4

echo "✔ pre-push checks passed"
